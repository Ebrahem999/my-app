import React from 'react';

const Maincontent = ({a}) => {
    return (
        <main>
        {a}<br />
        Menu Bar <br />
        HTML &amp; CSS only
      </main>
    );
}

export default Maincontent;
